window.PIXELSWITCH_CONFIG = {
  // Set this to your deployed API base, for example: https://api.example.com/api
  commentsApiUrl: '',
  // Optional endpoint for contact form submissions.
  contactApiUrl: '',
  // Leave empty for private per-browser counters. Set to the included API base
  // to show shared site-wide conversion and download totals.
  activityApiUrl: ''
};
